# 1. Use the lightweight Nginx web server
FROM nginx:alpine

# 2. Copy our HTML file into Nginx's default public folder
COPY index.html /usr/share/nginx/html/

# 3. Document that Nginx uses port 80 internally
EXPOSE 80